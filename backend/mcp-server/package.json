{
  "name": "@veta/mcp-server",
  "version": "1.0.0",
  "description": "VETA Trading Platform - Model Context Protocol Server",
  "type": "module",
  "main": "src/server.ts",
  "scripts": {
    "start": "deno run --allow-all src/server.ts",
    "test": "deno test --allow-all tests/"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  }
}
