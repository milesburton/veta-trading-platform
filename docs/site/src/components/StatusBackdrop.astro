---
---

<div class="veta-status-backdrop">
  <slot />
</div>

<style>
  .veta-status-backdrop {
    margin: -2rem -1.5rem;
    padding: 3rem 1.5rem;
    background:
      radial-gradient(ellipse 55% 45% at 12% 8%, var(--sl-color-purple-low), transparent 70%),
      rgb(var(--gray-950));
  }
  @media (min-width: 50rem) {
    .veta-status-backdrop {
      margin: -2rem -3rem;
      padding: 4rem 3rem;
    }
  }
</style>
