---
title: Observability
description: Metrics, logs and traces via the self-hosted LGTM stack on UAT.
---

import { Aside } from "@astrojs/starlight/components";
import Svc from "../../../components/Svc.astro";
import GrafanaScreenshots from "../../../components/GrafanaScreenshots.astro";

VETA emits metrics, logs and traces to a separate **LGTM** stack — Loki, Grafana, Tempo and Mimir/Prometheus, with Grafana Alloy collecting events. The trading stack runs fine without it; bring it up when investigating performance, debugging event flow, or generating data for the [Performance reference page](../../reference/performance/).

<Aside type="caution" title="UAT only — not for production">
The self-hosted LGTM stack and the OTel auto-instrumentation overlay described on this page are intended for the homelab/UAT environment. Production deployments should send OTLP to a managed observability vendor (Grafana Cloud, Honeycomb, Datadog, New Relic) rather than running their own collector + storage on the same host as the trading platform. Co-locating the observability backend with the workload it observes creates a noisy-neighbour failure mode where a metric spike crashes both the platform and the dashboard you would have used to diagnose it.

The instrumentation itself — `OTEL_DENO=true`, the trace-propagating Kafka helpers, the `withSpan()` calls — works against any OTLP receiver. Only the receiver and storage choice changes.
</Aside>

## LGTM stack

A separate Compose stack at [`observability/docker-compose.lgtm.yml`](https://github.com/milesburton/veta-trading-platform/blob/main/observability/docker-compose.lgtm.yml) provides metrics, logs and traces. It is independent of the trading stack and can run on the same host or be pointed at a remote one.

| Container | Image | Port | Role |
|-----------|-------|------|------|
| `lgtm-grafana` | `grafana/grafana:11.4.0` | 3000 | Dashboards, alerting, datasource UI |
| `lgtm-prometheus` | `prom/prometheus:v3.1.0` | 9090 | Time-series metrics; remote-write enabled |
| `lgtm-loki` | `grafana/loki:3.3.2` | 3100 | Log aggregation |
| `lgtm-tempo` | `grafana/tempo:2.7.1` | 3200 / 4319 | Distributed traces |
| `lgtm-alloy` | `grafana/alloy:v1.7.0` | 12345 | Log + metric collector; tails service stdout |

Grafana is provisioned with the three datasources and a `Trading` folder containing both the project-specific dashboard and the official k6 Prometheus dashboard.

```bash
docker compose -f observability/docker-compose.lgtm.yml up -d
```

Open `http://localhost:3000` once the stack is up. Anonymous read access is enabled for local development; production deployments should add auth.

## How events get into Loki

The platform's <Svc name="kafka-relay" /> subscribes to every Redpanda topic and forwards each event as a JSON line to stdout. Grafana Alloy tails those lines and ships them to Loki, giving Grafana a unified log search across the entire trading pipeline — including the order lifecycle, fills, algo decisions and risk events.

The relay also exposes `POST /events/batch` for the frontend. The browser's [error transport middleware](https://github.com/milesburton/veta-trading-platform/blob/main/frontend/src/store/middleware/errorTransportMiddleware.ts) posts uncaught errors here, so client-side failures show up in Loki alongside backend events.

## OpenTelemetry tracing

Every Deno service emits OTel traces and metrics when started with `OTEL_DENO=true`. The `compose.observability.yml` overlay enables this on the homelab and points each service at the Alloy collector:

```bash
docker compose -f compose.yml -f compose.prod.yml -f compose.observability.yml up -d
```

The overlay is opt-in — Fly and bare local development don't ship traces by default to keep the attack surface small and the bandwidth cost zero. Service names are auto-derived from the container hostname inside [`backend/src/lib/telemetry.ts`](https://github.com/milesburton/veta-trading-platform/blob/main/backend/src/lib/telemetry.ts), so each service appears in Tempo without having to plumb `OTEL_SERVICE_NAME` into 35 compose blocks individually.

Trace context propagates automatically:

- **HTTP**: Deno's auto-instrumentation injects/extracts `traceparent` on every `fetch` and `Deno.serve` call. So an order flowing Gateway → OMS → Risk Engine → EMS shows up as one connected trace in Tempo.
- **Kafka**: the producer in [`messaging.ts`](https://github.com/milesburton/veta-trading-platform/blob/main/backend/src/lib/messaging.ts) injects `traceparent` into Kafka message headers; the consumer extracts it before invoking the handler. So the chain extends across every bus hop too — `orders.routed → orders.child → orders.filled` all appear in one trace.

Custom business spans are added with the `withSpan()` helper:

```ts
import { withSpan } from "@veta/telemetry";

await withSpan("score-signal", async (span) => {
  span.setAttribute("symbol", symbol);
  return scoreSignal(features);
});
```

When `OTEL_DENO` is unset, `withSpan()` is a no-op zero-cost call — safe to leave in production code paths.

## Provisioned dashboards

Grafana is auto-provisioned with the following dashboards under the **Trading** folder. They live in [`observability/grafana/provisioning/dashboards/`](https://github.com/milesburton/veta-trading-platform/tree/main/observability/grafana/provisioning/dashboards) and update on disk-change without a Grafana restart.

| Dashboard | Source | What it shows |
|-----------|--------|---------------|
| **Trading Overview** | Loki via kafka-relay | Order pipeline counters, fill rate, algo heartbeat, service version table, live event feeds. Works without OTel — just needs the kafka-relay → Loki path. |
| **Service Performance (OTel)** | Tempo span-metrics + Loki | Pan-platform request rate, error rate, latency p50/p95/p99 per service. Top-N tables of slowest and busiest spans. Service-scoped log panel at the bottom. |
| **Order Pipeline (Traces)** | Tempo + Loki | Trace-driven view of the order lifecycle, including per-stage p95 latency across gateway/oms/risk/algos/ems/journal, a live nodeGraph from Tempo's service-graph processor, recent error and slow-trace tables. |
| **k6 Prometheus** | Prometheus | Bundled k6 dashboard for load-test runs. |

The two OTel dashboards depend on the `compose.observability.yml` overlay being active — without it there are no spans to render.

### Live snapshots

The screenshots below are captured directly from the homelab Grafana via its [image-renderer](https://grafana.com/docs/grafana/latest/setup-grafana/image-rendering/) by [`docs/site/scripts/capture-grafana-screenshots.mjs`](https://github.com/milesburton/veta-trading-platform/blob/main/docs/site/scripts/capture-grafana-screenshots.mjs). To refresh, run `npm run capture:grafana` from `docs/site/` against a host with the LGTM stack up. Click any panel for the full-resolution version.

<GrafanaScreenshots />

## In-app log search

The trading UI's header has a **Logs** button that opens a drawer for searching backend log output without leaving the platform. It hits `GET /api/gateway/logs/query` with optional `service`, `level`, `q` (free-text), `since` (5m/15m/1h/6h) and `limit` parameters.

The gateway resolves the query in this order:

1. **Loki** if `LOKI_URL` is configured on the gateway (typical UAT setup) — the full LogQL search runs against every service's structured log stream.
2. **In-process ring buffer** otherwise — the gateway records its own log lines into a 2,000-entry buffer via `registerLogSink()` so at least the gateway's view is searchable when no external log store is available. This is the Fly demo's mode.

The route is gated to non-viewer roles. Logs may contain user identifiers and other sensitive context, so external-clients and viewers see a 403 when they hit the endpoint directly.

## Pipeline-stage latency

The <Svc name="journal" /> service exposes `GET /metrics/latency` returning per-stage `p50`/`p95`/`p99` for the order pipeline (`gateway → bus → oms → routedToChild → emsAck → filled`). Recent baselines and how they were measured live on the [Performance page](../../reference/performance/).

## Session replay

The <Svc name="replay-service" /> records full browser sessions via [rrweb](https://www.rrweb.io/) when an admin enables recording. Each row in the Session Replay panel offers three exports:

- **Play** — open the rrweb player inline.
- **JSON** — download the raw event stream (replays in any rrweb player).
- **Video** — render a WebM in the browser using `MediaRecorder`. The renderer steps through the rrweb events, snapshots each frame at 10 fps, and encodes via VP9/VP8. Runs entirely client-side (no server-side ffmpeg or headless Chromium), which keeps the attack surface small at the cost of needing the operator's tab to stay open during render. WebM plays in every modern browser, Slack, and most video tools.

## Where to run what

| Environment | Trading stack | LGTM stack | k6 | Watchtower |
|-------------|---------------|-----------|----|-----------|
| `local` (devcontainer) | yes | optional | optional | no |
| `uat` (private VM) | yes | yes | manual | yes |
| `fly` (public demo) | monolith | no | manual against URL | no |

LGTM and k6 are deliberately optional locally — they add real overhead and the platform runs fine without them.
