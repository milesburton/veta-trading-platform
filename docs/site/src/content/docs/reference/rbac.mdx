---
title: RBAC & Permissions
description: Role-based access control for panels, orders, and admin functions.
---

import RbacTable from "../../../components/RbacTable.astro";

{/*The role rows and `Can trade` column are generated from frontend/src/auth/rbac.ts by docs/site/scripts/generate-rbac.mjs. The `Can admin` and `Panel access` columns live in the RbacTable component because they describe panel-permission policy that spans PANEL_PERMISSIONS and PANEL_TRADING_STYLES, not rbac.ts.*/}

## Roles

<RbacTable />

## Panel permissions

Every panel has a `PANEL_PERMISSIONS` entry defining which roles can access it, and an optional `PANEL_TRADING_STYLES` entry further restricting trader access by style.

The `canAccessPanel(panelId, role, tradingStyle)` function is the single source of truth, used by:

- **ComponentPicker**: filters the "Add Panel" dropdown
- **DashboardLayout factory**: renders "no permission" message for unauthorised panels in saved layouts

## Enforcement points

1. **Frontend ComponentPicker**: panel not shown if role/style does not match
2. **Frontend DashboardLayout factory**: unauthorised panel renders denial message
3. **Frontend Order Ticket role-check**: blocks order submission with per-role/per-style messages
4. **Backend OMS**: rejects orders from non-trader roles
5. **Backend OMS**: rejects orders failing risk-engine checks
